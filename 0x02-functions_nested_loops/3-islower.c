#include "main.h"
/**
* _islower - prints 1 or 0 depending on input add
*
* @ch: first parameter
*
* Description: prints lowercase
*
* Return: 0 or 1
*/
int _islower(int ch)
{
return (ch >= 'a' && ch <= 'z');
}
